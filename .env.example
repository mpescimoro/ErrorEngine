# ============================================
# ErrorEngine - Configurazione
# ============================================
# Copia questo file come .env e modifica i valori

# === DATABASE ===
# Configure DB connections through the web UI (Connessioni DB)
# Remember to remove the '#' from the lines for the databases you want to install in requirements.txt

# === EMAIL (SMTP/Exchange) ===
MAIL_SERVER=smtp.office365.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=sender@yourdomain.com
MAIL_PASSWORD=your_email_password
MAIL_DEFAULT_SENDER=noreply@yourdomain.com

# === FLASK ===
# Genera una chiave segreta casuale per la produzione
SECRET_KEY=change-this-to-a-random-string

# === OPZIONALE ===
# LOG_LEVEL=INFO
# FLASK_ENV=production
# HTTP_TIMEOUT_SECONDS=30

# === RETENTION (giorni) ===
# LOG_RETENTION_DAYS=30
# EMAIL_LOG_RETENTION_DAYS=90
# RESOLVED_ERRORS_RETENTION_DAYS=60

# Timezone per visualizzazione date (default: UTC)
# Esempi: Europe/Rome, America/New_York, Asia/Tokyo
TIMEZONE=Europe/Rome
